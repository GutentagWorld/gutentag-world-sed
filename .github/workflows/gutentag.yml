name: Gutentag, World!

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v5

      - name: Run
        run: echo | sed -f gutentag_world.sed

      - name: Verify output
        run: bash test.sh
